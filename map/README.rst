============================================
Example app for `django-dash`
============================================
Contains a map plugin example.

Usage
============================================
- Add ``Map`` plugin to your dashboard.

- Use the following data as URL value:

DEFAULT_URL_VALUE = "http://vmap0.tiles.osgeo.org/wms/vmap0"
